# Shared secret the screener sends as the x-ingest-secret header.
# Generate with: openssl rand -hex 32
INGEST_SECRET=

# Injected automatically by Vercel once a Blob store is connected to the
# project — you do not set these by hand in production.
# BLOB_READ_WRITE_TOKEN=
# BLOB_STORE_ID=
